import {eventType} from './eventType'
import {artistType} from './artistType'
import {venueType} from './venueType'
import {albumType} from './albumType'

export const schemaTypes = [artistType, eventType, venueType, albumType]
